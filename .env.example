# Pick one of the providers (OpenAI recommended) and set the key.
OPENAI_API_KEY=
# or:
ANTHROPIC_API_KEY=
# or use OpenRouter (optional alternative aggregator)
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Optional: Tavily for web search (free tier available)
TAVILY_API_KEY=

# Default model (override in UI if needed)
AI_MODEL=gpt-4o-mini
